import * as React from 'react';
import './TimelineGroup.scss';

interface TimelineGroupProps {
    timelineItems: JSX.Element[]
}

const TimelineGroup: React.FC<TimelineGroupProps> = (props) => {

    return (
        <div className={'rsTimelineGroup'}>

        </div>
    );
};
export default TimelineGroup;